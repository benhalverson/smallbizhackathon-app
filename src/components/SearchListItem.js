import React from 'react';
import List from '@material-ui/core/List';
const SearchListItem = ({ title, description, image, id }) => (
  <div>An Item goes here...</div>
);

export default SearchListItem;
